<h2>Create a task</h2>

<%= form_for @task, :html => {:class => "form-horizontal center"} do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit %>
<% end %>